<UserControl x:Class="Cims.WorkflowLib.Wpf.UserControls.DbGrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cims.WorkflowLib.Wpf.UserControls">
    <Grid Name="grBrowse" Background="Gray" HorizontalAlignment="Left">
        <ScrollViewer ScrollViewer.CanContentScroll="True" 
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid Name="dgrBrowse" 
                    Background="Transparent"
                    HorizontalAlignment="Left"
                    AutoGeneratingColumn="dgrBrowse_AutoGeneratingColumn" />
        </ScrollViewer>
    </Grid>
</UserControl>
