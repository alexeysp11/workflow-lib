<UserControl x:Class="Cims.WorkflowLib.Wpf.UserControls.MultilineTextBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cims.WorkflowLib.Wpf.UserControls">
    <Grid VerticalAlignment="Top" HorizontalAlignment="Left">
        <Border Name="brdMultiline" BorderBrush="Black" BorderThickness="0.5">
            <TextBox Name="tbMultiline" 
                TextWrapping="Wrap" 
                FontFamily="Courier New" 
                AcceptsReturn="True" 
                AcceptsTab="True"
                VerticalScrollBarVisibility="Visible" 
                HorizontalScrollBarVisibility="Auto"  />
        </Border>
    </Grid>
</UserControl>
